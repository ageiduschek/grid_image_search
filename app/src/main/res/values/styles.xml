<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="LightboxActivityTheme" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/LightboxActionBarTheme</item>
        <item name="actionBarStyle">@style/LightboxActionBarTheme</item>
    </style>


    <!-- ActionBar styles -->
    <style name="LightboxActionBarTheme"
           parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/black</item>
        <item name="background">@color/black</item>
    </style>
</resources>
